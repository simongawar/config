
spring:
  application:
    name: config-server
  profiles:
    active:
      - native, git
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        # Uncomment and configure Git if needed
        # git:
        #   uri: https://github.com/ihuaylupo/config.git
        #   searchPaths: licensingservice

# Server settings
server:
  port: 8071

# Encryption key for Spring Cloud Config
encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo

# Enable all Spring Boot Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"
